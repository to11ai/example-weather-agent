# Provider key — still set in step 02; the gateway forwards it upstream.
OPENAI_API_KEY=

# to11 gateway auth (sent as headers; no to11 SDK required)
TO11_API_KEY=
TO11_PROJECT_ID=
TO11_GATEWAY_URL=https://gw.to11.ai/v1
TO11_ENV=prod
